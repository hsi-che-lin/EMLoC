{
    "overwrite_output_dir": true,
    "group_by_length": true,
    "bf16": true,
    "max_steps": 500,
    "save_steps": 500,
    "per_device_train_batch_size": 2,
    "gradient_accumulation_steps": 4,
    "learning_rate": 4e-5,
    "lr_scheduler_type": "cosine",
    "warmup_ratio": 0.03,
    "weight_decay": 0.01,
    "logging_steps": 1,
    "save_total_limit": 1,
    "dataloader_num_workers": 4,
    "save_strategy": "steps",
    "evaluation_strategy": "no",
    "report_to": "none",
    "deepspeed": "config/deepspeed/default.json"
}